export default {
  base: '/',                 // URLs absolues depuis la racine du site
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    manifest: true           // génère dist/manifest.json
  }
}